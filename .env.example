# Only used when building the Android app (npm run build:app). Never put secrets here —
# this value is compiled into the APK and is public.
VITE_API_BASE=https://hsn-autoreply.YOUR-SUBDOMAIN.workers.dev
